# IOC Manager Framework - Runtime Requirements
# Install with: pip install -e ".[all]" for full functionality

# Core (always required)
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pyyaml>=6.0
pydantic>=2.0

# Optional: EPICS soft IOC support
softioc
cothread

# Optional: Ophyd device abstraction
# ophyd
infn_ophyd_hal
epik8s-tools
p4p
# Optional: Kubernetes integration
# kubernetes

# Development
pytest>=7.0
pytest-asyncio
httpx
black
flake8